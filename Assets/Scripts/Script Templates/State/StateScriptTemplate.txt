// N'oublie pas d'enregistrer cet etat dans PlayerStateMachine : ajoute une propriete
// typee et sa ligne Register(...) dans le constructeur, sinon il reste inatteignable.
public class #SCRIPTNAME# : PlayerState
{
    public #SCRIPTNAME#(PlayerStateMachine stateMachine) : base(stateMachine){}
    protected override string StateAnimationName => "#ANIMATIONNAME#";
        
    public override void RegisterTransition()
    {
        // Register your state transitions here
        // AddTransition(() => condition, stateMachine.TargetState);
    }
    
    public override void Update()
    {
        CheckTransitions();
    }
    
    public override void Enter()
    {
        base.Enter(); // joue StateAnimationName : sans cet appel, l'animation ne part pas
    }

    public override void Exit()
    {
        base.Exit();
    }
}
